<%
  let first = true;
  parameters.forEach((parameter) => {

    if (!first) {
      %>, <%
    } else {
      first = false;
    }
    
    %><%= parameter.name %>: <%= parameter.schema.type %><%
  });
%>